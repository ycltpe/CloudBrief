---
updated: 2026-07-07T00:09
---

- (dynamic) Yechen wants to rework Chat.tsx into a left-history / right-chat layout and switch the conversation from JSON POST to SSE streaming. Scope: front-end Chat.tsx + back-end together; conversation list API to be added if missing.
- (moment) Backend scan confirmed: no conversation list endpoint, no user scoping on conversations, ModelClient already supports stream=True internally, ChatService uses stream=False. Gap list is now shared context.
- (outcome) Yechen decided: (1) conversation title = first-message truncation + editable; (2) history list scoped to current user; (3) SSE interruption surfaces a user-triggered retry prompt rather than auto-retry.
- (outcome) Yechen confirmed two-PR approach: PR1 = left history list + user-scoped conversations + inline-editable titles; PR2 = SSE streaming. Title editing is inline, not dialog.
- (dynamic) Room agreed to pause implementation and let Sally produce a UI wireframe/spec before PR1 coding begins. Yechen chose design-first.
- (outcome) Design approved by Yechen. Party moved to implementation: PR1 to be built by Amelia/Mimir. PR2 (SSE streaming) queued after PR1 is verified.
- (outcome) PR1 implemented and verified: DB migration, user-scoped conversation list, inline title editing, left/right Chat.tsx layout. All backend tests pass; frontend build succeeds. PR2 (SSE streaming) remains queued.
- (outcome) 圆桌围绕 'AI+房产' 中用户输入小区名（如'和合苑'）的场景吵出了 PRD：先做城市级消歧，再输出可解释的小区画像卡片，所有数据必须标注来源与更新时间，MVP 限定上海。John 坚持 JTBD 优先，Winston 把消歧列为架构不可协商项，Amelia 强行定了 8 条可验收 AC。产物落盘 _bmad-output/party-mode/prd-ai-real-estate-community-query-2026-07-06.md。
- (outcome by assistant) Implemented friendly stream status prompts: backend yields status events (thinking/generating) in ChatService.ask_stream, frontend Chat.tsx displays them in the assistant placeholder and clears on first chunk. Backend tests pass (19) and frontend build succeeds.
- (outcome by assistant) Fixed citation visibility: status now shows retrieved source count; added CitationSources component below assistant answers for an expandable source list. Verified backend tests and frontend build.
- (outcome by assistant) Fixed citation marker parsing in AnswerWithCitations: now recognizes raw chunk_id markers like [^help_doc:help_docs/export-guide.md:0] by matching chunk_id to citations array, while keeping backward compatibility with numeric [^1] markers. Frontend build passes.
- (outcome by assistant) Added InlineMarkdown component to render **bold**, *italic*, and inline code in user messages; ** is standard Markdown bold syntax. Frontend build passes.
- (outcome by assistant) Extended InlineMarkdown to assistant answers: AnswerWithCitations now renders non-citation text segments through InlineMarkdown, so **bold** and other inline Markdown is parsed in both user and assistant messages. Build passes.
- (outcome by assistant) Unified frontend timezone to Asia/Shanghai: added lib/datetime.ts with formatTime/formatDate/formatDateTime/formatIsoDate/formatChatDateLabel, replaced all direct toLocaleString and replace('T') usages across Chat, IndexRebuildPanel, admin dashboard/eval/kb/users, and citation components. Naive UTC ISO strings from backend are parsed as UTC before formatting. Frontend build passes.
- (outcome) Yechen 要求给全前端加暗黑/明亮模式切换。房间吵完范围后 Mimir 直接开干：next-themes + Tailwind darkMode class，语义化 CSS 变量，ThemeToggle 组件，AdminLayout/后台各页/Chat/IndexRebuildPanel/AnswerWithCitations 全部迁移。npm run build 通过。README 加了主题模式说明。
- (moment by bmad-agent-pm) John 把 Yechen 的要求钉成房间规则：以后本项目新增前端页面必须同时支持明亮和暗黑模式。
